# Local dev (default): file:data.db — no env vars needed
# LIBSQL_URL=file:data.db

# Vercel production: create a free Turso DB and set these in Vercel project settings
# TURSO_DATABASE_URL=libsql://your-db-name.turso.io
# TURSO_AUTH_TOKEN=your-auth-token
